# Input

The application uses three predefined text values as input.

# Process

React Native renders one View and three Text components with a yellow background.

# Output

The app displays the name, degree, and school centered on the screen.
